# App
PORT=5000
CORS_ORIGIN=http://localhost:5173
JWT_SECRET=replace_with_strong_secret

# Database (Option A: single URL)
# DB_URL=postgres://user:pass@localhost:5432/mydb

# Database (Option B: discrete fields)
DB_DIALECT=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=signalz
DB_USER=postgres
DB_PASS=Tekam@001

# Optional SSL (enable if your provider requires SSL)
# DB_SSL=true
# DB_SSL_REJECT_UNAUTHORIZED=false
